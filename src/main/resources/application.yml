spring:
  profiles:
    active: ${APP_PROFILE:jpa}
  main:
    banner-mode: off
server:
  port: ${PORT:8080}
jwt:
  secret: ${JWT_SECRET:twoj-sekret-base64url-minimum-32-znaki}
  expiration: ${JWT_EXPIRATION:3600000}
